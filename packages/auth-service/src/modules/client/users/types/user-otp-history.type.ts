export enum IsExpiredOtpType {
    Init = 0,
    verified = 1,
    Used = 2,
}

export enum SendViaOtpType {
    Email = 1,
    PhoneNumber = 2,
}

export enum UserOtpHistoryType {
    Verified = 1,
}
